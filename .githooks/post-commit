#!/usr/bin/env bash
# README.md 由 GitHub Actions 维护（见 .github/workflows/update-skills-readme.yml）。
# 本地 commit 后请 push，再 git pull 获取 CI 生成的索引更新。
# 本地预览：python3 scripts/generate-skills-readme.py
exit 0
